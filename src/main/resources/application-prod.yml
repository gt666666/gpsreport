spring:
  # spring data for mongodb
  data:
    mongodb:
      host: 127.0.0.1

  # redis 缓存服务器配置
  redis:
    # 服务地址
    host: 127.0.0.1
    # 登录密码
    password: Redis_kmzx@1234

logging:
  level:
    com:
      ynzhongxi:
        gpsreport: debug
  file: /opt/logs/pgs_report/log.log